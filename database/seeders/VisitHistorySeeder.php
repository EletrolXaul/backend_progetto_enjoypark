<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use App\Models\VisitHistory;

class VisitHistorySeeder extends Seeder
{
    public function run()
    {
        // Seeder vuoto, pronto per dati futuri.
        // Esempio di inserimento (commentato):
        /*
        VisitHistory::create([
            'user_id' => 1,
            'location_id' => 5,
            'visited_at' => now(),
            'notes' => 'Visita pomeridiana',
        ]);
        */
    }
}
